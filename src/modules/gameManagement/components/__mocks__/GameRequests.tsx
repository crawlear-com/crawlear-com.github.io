import * as React from 'react'
import { User } from "../../../social/User"

interface GameRequestsProps {
    user: User
}

const GameRequests = ({user}: GameRequestsProps) => {
    return <div>GameRequests</div>
}

export default GameRequests