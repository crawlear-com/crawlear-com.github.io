import React from "react"
import { GameListProps } from "../GameList"

const GameList = ({ games, readOnly, onRemoveGame, onConfigureGame, title, onGamePlay }: GameListProps) => {
    return <div title="GameList">GameList</div>
}

export default GameList
