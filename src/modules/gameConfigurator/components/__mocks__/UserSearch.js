const UserSearch = ({onUserSeachPlayerAdd, 
    onUserClick,
    onPlusAddUserClick,
    mainText,
    secondaryText,
    iconSend,
    children}) => {
        return <div>UserSearch</div>
}

export default UserSearch