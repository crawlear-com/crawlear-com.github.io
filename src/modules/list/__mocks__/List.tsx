import * as React from 'react'
import { ListProps } from "../List"

const List = ({ data, readOnly, onRemoveItem, onConfigureItem, title, onItemAction, transformer}: ListProps) => {
    return <div> List </div>
}

export default List